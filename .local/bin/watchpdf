#!/bin/sh

FILE="$1"

sioyek "$FILE.pdf" &

while inotifywait -e modify -qq "$FILE.tex"; do
  texfot pdflatex joris-resume.tex \
      ; sioyek --nofocus --execute-command reload > /dev/null 2>&1
done
