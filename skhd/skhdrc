# I use skhd and yabai to create a window manager similar to i3 on
# Linux. However, for some reason, I do not like using a tiling window
# manager on Mac. Therefore I am not actively using or maintaining these
# configuration.
# https://github.com/koekeishiya/skhd
# https://github.com/koekeishiya/yabai

# new terminal
ctrl - return : open -n -a iTerm

# focus movement
shift + ctrl - h : yabai -m window --focus west
# shift + ctrl - h : yabai -m window --swap west

# ctrl - j : yabai -m window --focus south
# shift + ctrl - j : yabai -m window --swap south

# ctrl - k : yabai -m window --focus north
# shift + ctrl - k : yabai -m window --swap north

shift + ctrl - l : yabai -m window --focus east
# shift + ctrl - l : yabai -m window --warp east

# fullscreen
# ctrl - f : yabai -m window --toggle zoom-fullscreen

# only in bsp mode
# shift + ctrl - space : yabai -m window --toggle float

# spaces
# make a new space and eradicate a space
ctrl - m : yabai -m space --create
ctrl - e : yabai -m space --destroy

ctrl - 1 : yabai -m space --focus 1
shift + ctrl - 1 : yabai -m window --space 1

ctrl - 2 : yabai -m space --focus 2
shift + ctrl - 2 : yabai -m window --space 2

ctrl - 3 : yabai -m space --focus 3
shift + ctrl - 3 : yabai -m window --space 3

ctrl - 4 : yabai -m space --focus 4
shift + ctrl - 4 : yabai -m window --space 4
